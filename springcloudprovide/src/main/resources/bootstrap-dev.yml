#注册到服务中心
eureka:
  instance:
    hostname: 118.25.54.103
  client:
    region: myZone
    availability-zones:
      myZone: devZone,proZone

    service-url:
      devZone: http://${eureka.instance.hostname}:${springcloudeureka.dev.port}/eureka/
      proZone: http://${eureka.instance.hostname}:${springcloudeureka.pro.port}/eureka/

